# NFL Statistics Comparison Platform

A full-stack web application for analyzing and comparing NFL player and team statistics.

## Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         Frontend                                 │
│              Next.js 16 / React 19 / Tailwind v4                │
│                    /stats-app                                    │
└─────────────────────┬───────────────────────────────────────────┘
                      │ Server Components (direct DB queries)
                      ▼
┌─────────────────────────────────────────────────────────────────┐
│                      PostgreSQL                                  │
│          Hosted on VPS (AWS Lightsail)                          │
│   Access: SSH tunnel (dev) or direct localhost (prod)           │
└─────────────────────────────────────────────────────────────────┘
                      ▲
                      │ ETL Pipeline (R)
                      │
┌─────────────────────────────────────────────────────────────────┐
│                    nflverse API                                  │
│            Play-by-play data source                             │
└─────────────────────────────────────────────────────────────────┘
```

## Directory Structure

```
stat-compare/
├── stats-app/          # Next.js frontend (Phase 3 complete)
├── database/           # PostgreSQL schema, functions, setup
├── etl/                # R-based ETL pipeline
└── .project-summary.md # This file
```

---

## Database (`/database`)

### Tables
| Table | Primary Key | Purpose |
|-------|-------------|---------|
| `players` | `player_id` | Player names, career span |
| `teams` | `team_id` (3-char) | Team info, colors |
| `weeks` | `(season, week)` | Season type (REG/POST) |
| `player_week` | `(player_id, season, week)` | Individual game stats (~50 cols) |
| `team_week` | `(team_id, season, week)` | Team game stats (~70 cols) |

### Key Functions
- **`player_stats(player_id, season_start, season_end, week_start, week_end, season_type)`**
  - Returns ~142 aggregated columns (passing, rushing, receiving, totals, fantasy)
  - All params optional (NULL = no filter)

- **`team_stats(team_id, ...same params...)`**
  - Returns ~252 aggregated columns (offense + defense mirrors)

### Connection
- **Development:** SSH tunnel → `localhost:5433`
- **Production:** Direct on VPS → `localhost:5432`

---

## ETL Pipeline (`/etl`)

R-based pipeline using `nflreadr` package.

**Data Flow:** nflverse API → Extract → Transform → Load → PostgreSQL

**Coverage:** 1999-present, ~40 player metrics/week, ~60 team metrics/week

**Positions Tracked:** QB, WR, RB, TE

**Key Files:**
- `src/etl_pipeline.R` - Main orchestrator
- `src/transform_players.R` - Player stat calculations
- `src/transform_teams.R` - Team stat calculations

---

## Frontend (`/stats-app`)

### Tech Stack
- Next.js 16 (App Router), React 19, Tailwind CSS v4, TypeScript
- `pg` for PostgreSQL connection (no ORM)
- `nuqs` for URL state sync, `zod` for validation

### Current Structure (Phase 3 Complete)
```
stats-app/
├── app/
│   ├── api/search/route.ts        # GET /api/search?q=
│   ├── players/
│   │   ├── page.tsx               # Placeholder listing
│   │   └── [id]/page.tsx          # Player stats (7 tables, ~142 stats)
│   ├── teams/
│   │   ├── page.tsx               # Placeholder listing
│   │   └── [id]/page.tsx          # Team stats (12 tables, ~252 stats)
│   ├── compare/page.tsx           # Placeholder
│   ├── graph/page.tsx             # Placeholder
│   ├── dictionary/page.tsx        # Placeholder
│   ├── layout.tsx                 # Root layout: Navbar + DateRangeFilter + NuqsAdapter
│   ├── page.tsx                   # Landing page
│   └── globals.css                # Tailwind v4 config, NFL color vars
├── components/
│   ├── Navbar.tsx                 # Sticky nav, search, mobile hamburger
│   ├── SearchBar.tsx              # Live autocomplete dropdown
│   ├── filters/
│   │   ├── DateRangeFilter.tsx    # Global filter bar (year, week, season type)
│   │   ├── YearSelect.tsx         # Year dropdown (1999–2025)
│   │   ├── WeekSelect.tsx         # Week dropdown (1–22)
│   │   └── SeasonTypeToggle.tsx   # REG/POST/Both toggle
│   ├── players/
│   │   ├── StatSection.tsx        # Reusable horizontal scrollable stat table
│   │   └── PlayerStatsDisplay.tsx # Position-aware stat display (7 sections)
│   └── teams/
│       └── TeamStatsDisplay.tsx   # Off/Def stat display (12 sections)
├── lib/
│   ├── db.ts                      # pg Pool + query helpers
│   ├── types.ts                   # Re-exports + Player, Team, SearchResult
│   ├── types/
│   │   ├── player-stats.ts        # PlayerStats interface (~142 cols)
│   │   └── team-stats.ts          # TeamStats interface (~252 cols)
│   ├── filters/
│   │   ├── search-params.ts       # nuqs parsers, searchParamsCache, constants
│   │   └── validation.ts          # Zod schema + toDbParams()
│   └── utils/cn.ts                # clsx + tailwind-merge
├── .env.local
└── package.json
```

### Filter System
- URL params: `startYear`, `startWeek`, `endYear`, `endWeek`, `seasonType`
- Defaults: 2025, Wk 1–22, REG (cleared from URL when default)
- Shared parsers between client (`useQueryStates`) and server (`searchParamsCache`)
- `shallow: false` triggers server re-render on filter change

---

## Implementation Plan

### Phase 1: Foundation ✅ Complete
- Database connection layer
- TypeScript types for all player and team stats
- Search API route with Zod validation

### Phase 2: Navigation & Search ✅ Complete
- Navbar with responsive hamburger menu
- SearchBar with live autocomplete (debounced, keyboard nav)
- Landing page with section link cards

### Phase 3: Date Range Filters & Detail Pages ✅ Complete
- `DateRangeFilter` — global filter bar (year, week, season type pickers)
- Filter state synced to URL via `nuqs` (shareable links)
- Player detail page — 7 horizontal scrollable stat tables (Passing, Adv Passing, Rushing, Adv Rushing, Receiving, Adv Receiving, Totals)
- Team detail page — 12 horizontal scrollable stat tables (Off/Def × Passing, Adv Passing, Rushing, Adv Rushing, Total, Situational)
- Position-aware display (QB/RB/WR/TE show different section ordering)

### Phase 4: Comparison Builder (Pending)
- Select 2-4 players or teams
- Pick specific stats to compare
- Side-by-side Stathead-style table

### Phase 5: Chart Builder (Pending)
- Scatter plots with team logos
- X/Y axis stat selection
- All 32 teams or custom player selection

### Phase 6: Data Dictionary (Pending)
- Stat definitions, formulas, descriptions
- Organized by category

### Phase 7: Polish (Pending)
- Loading states, error handling
- Accessibility, security hardening

---

## Environment Variables

```env
DB_HOST=localhost
DB_PORT=5433          # 5432 for production
DB_NAME=stats_db
DB_USER=stats_user
DB_PASSWORD=<secret>
DB_SSL=false
```

---

## Running Locally

```bash
# 1. Start SSH tunnel (separate terminal)
cd database && ./scripts/ssh-tunnel.sh

# 2. Set up environment
cd stats-app
cp .env.example .env.local
# Edit .env.local with credentials

# 3. Install and run
npm install
npm run dev

# 4. Test
curl http://localhost:3000/api/search?q=mahomes
# Or navigate to /players/<id> or /teams/KC in browser
```

---

## Key Design Decisions

1. **`pg` over Prisma** - Database already has stored procedures; ORM adds unnecessary abstraction
2. **URL state for filters** - Shareable links, SSR-compatible via nuqs
3. **No authentication** - Public tool, all features accessible
4. **Server Components** - Detail pages query DB directly, no API layer needed
5. **Minimal design** - Clean, focused UI; NFL Navy/Red theme
6. **Horizontal scrollable tables** - Fits many stats without vertical bloat
7. **Position-aware display** - Shows relevant stat sections first based on player position
